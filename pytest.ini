[pytest]
# pytest-asyncio is unrelated to this synchronous lab.  Some classroom/global
# Python environments contain an old copy that is incompatible with pytest 9.
addopts = -p no:asyncio
testpaths = tests

